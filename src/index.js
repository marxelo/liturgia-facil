import React from 'react';
import ReactDOM from 'react-dom/client';
import LiturgiaApp from './LiturgiaApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <LiturgiaApp />
  </React.StrictMode>
);
